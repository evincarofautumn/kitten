data vector<A> { /* ... */ }

data int { /* ... */ }

data io { /* ... */ }

def call<R..., S..., +E> (R..., (R... -> S... +E) -> S... +E):
  _::magic

def push_back<A> (vector<A>, A -> vector<A>):
  _::magic

def pop_back<A> (vector<A> -> optional<pair<vector<A>, A>>):
  _::magic

def foo (->) {
  [1, 2, 3]
}

data pair<A>:
  case pair (A, A)

data optional<A>:
  case none
  case some (A)

def + (int, int -> int) { /* ... */ }
infix right 6 +

def - (int, int -> int) { /* ... */ }
infix left 6 -

def * (int, int -> int) { /* ... */ }
infix left 7 *

def / (int, int -> int) { /* ... */ }
infix left 7 /

def >= (int, int -> int) { /* ... */ }
def >> (int, int -> int) { /* ... */ }
def >>= (int, int -> int) { /* ... */ }
def >>> (int, int -> int) { /* ... */ }
def foo1 (int, int -> int) { 1 >> 2 }
def foo2 (int, int -> int) { 1 >>> 2 }

def sqrt (int -> int) { /* ... */ }

def say<a> (a -> +io) { /* ... */ }

def scope (-> +io) {
  1000 -> x1;
  100 -> y1;
  10
  {
    -> a1;
    (a1 + x1)
    {
      -> b1;
      b1 + y1
    } call
  } call
  say
  1000 -> x2;
  100 -> y2;
  10
  {
    -> a2;
    (a2 + y2)
    {
      -> b2;
      b2 + x2
    } call
  } call
  say
}

vocab foo::bar {

def quadratic (int, int, int -> int):
  -> a b c;
  (a + (b * b - 4 * a * c) sqrt) / (2 * a)

}

def concat<A> (vector<vector<A>> -> vector<A>) { /* ... */ }

def map<R..., A, B, +E> (R..., vector<A>, <S...> (S..., A -> S..., B +E) -> R..., vector<B> +E):
  -> xs f;
  match (xs pop_back):
    case optional::some -> ys y:
      ys f map
      y f call
      push_back
    case optional::none: []

/*

def map<a, b, +e> (vector a, (a -> b +e) -> vector b +e):
  -> $xs $f;
  $xs init_last { $f map } { $f call } both

def map<a, b, +e> (vector a, (a -> b +e) -> vector b +e):
  -> xs f;
  xs init_last { f map } { f call } both

define dup<S...,A,+E> (S..., A -> S..., A, A +E):
  ->x;
  x
  x


data maybe<T>:
  case nothing ()
  case just (T)

define hello<S...> (S...->S...+io):
  if (okay):
    "hello world"
  else:
    "goodbye world"
  say

hello
  "\
" 0b1010 blah
*/

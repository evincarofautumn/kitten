data Void {}

data Unit: case Unit;

data Option {
  case None;
  case Some int;
}

data Either {
  case Left int;
  case Right [char];
}

data These:
  case This int;
  case That char;
  case These int char;

Unit match:
  case Unit:
    "Unit" say

1 Left LR
"foo" Right LR

define LR (Either ->):
  match:
    case Left: drop "Left" say
    case Right: drop "Right" say

1 Some
1 'x' These

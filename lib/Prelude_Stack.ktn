// Duplicates a value.
def dup (a -> a a):
  -> x
  x x

// Operator alias for 'dup'.
def <> (a -> a a):
  dup

// Drops a value.
def drop (a ->):
  -> x

// Swaps two values. Useful for changing argument order when
// partially applying a function.
def swap (a b -> b a):
  -> { a b }
  b a

// Operator alias for 'swap'.
def >< (a b -> b a):
  swap
